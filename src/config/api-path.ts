export enum AccountPath {
  Base = '/account',
}
export enum AuthPath {
  Name = 'Auth',
  Base = '/auth',
  Login = '/login',
  Register = '/register',
  Refresh = '/refresh',
  Logout = '/logout',
}

export enum AuthGooglePath {
  Login = '/login',
  Callback = '/callback',
}
