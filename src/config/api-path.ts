export enum AccountPath {
  Base = '/account',
}
export enum AuthPath {
  Name = 'Auth',
  Base = '/auth',
  Login = '/login',
  Register = '/register',
  GoogleLogin = '/google/login',
  GoogleCallback = '/google/callback',
}
